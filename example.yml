version: '0.2.0'
steps:
    MyDockerImage:
        title: Building Docker Image
        type: build
        image_name: ${{QA_IMAGE}}
    BumpBuildNumber:
        title: Bump Build Version Number
        image: codefreshplugins/bump-version-number
        env:
            - ANNOTATION_PREFIX=${{CF_BRANCH}}
        