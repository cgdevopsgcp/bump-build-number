version: '0.2.0'
steps:
  BuildImage:
    title: Building Docker Image
    type: build
    image_name: codefreshplugins/bump-version-number
  PushingToRegistry:
    type: push
    title: Pushing To Registry
    candidate: ${{BuildImage}}
    tags:
      - "0.2.0"
      - "latest"